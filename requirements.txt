# PC (development / training)
# pyproject.toml + uv.lock are authoritative for the trainer (uv run / uv sync);
# this file is kept in step as a plain-pip fallback.
tensorflow>=2.16
opencv-python
numpy
pyserial
flask

# Pi (inference only) — instead of tensorflow:
# tflite-runtime
# opencv-python-headless
# picamera2  (via apt: python3-picamera2)
